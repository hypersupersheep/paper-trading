# 协作交接

各 agent 之间**交接任务、汇报进度、共享记忆**的唯一公共文件。放在 app 仓库根,所有挂了本仓库的 agent 都能直接读写。

---

## 署名制度

- 每条记录**必须署名**:谁做的就署谁的代号,例:`— QR-A`。
- 格式:`### [日期] 标题 — 代号`,正文写「做了什么 / 给对方的需求 / 待确认」。
- 给别人派活/提需求,在条目里写清「**@对方代号:需要你做…**」;对方完成后**在同一文件追加回执并署名**。
- 不删别人的记录(append-only);要更正就追加一条说明。

## 成员 / 角色

| 代号 | 角色 | 负责 |
|------|------|------|
| **QR-A** | paper-trading app(节点侧) | 节点 app 全部功能、对接 Admin 的节点端实现、发版 |
| **Admin** | Admin 监控枢纽 | 监控墙、节点轮询/反控、账户级聚合;详见 `ADMIN_HANDOFF.md` |

> 历史的双向对接细节(账户级登记 / 节点鉴权 / SSE / 零配置直连)见 `ADMIN_HANDOFF.md`(Admin 写)+ `APP_HANDOFF.md`(QR-A 写),v1–v7 已闭环。本文件用于**新任务**的协作。

---

## 进度 / 记忆共享

- 节点 app 当前版本:见 `backend/version.py`(发版三件套:GitHub Release + 本地 app + Codex skill 同步)。
- 老板机(默认 Admin 地址):`http://192.168.0.58:8800`,已内置进 app,新机器零配置直连。

---

## 任务记录

### [2026-06-25] 策略描述功能(文字 + 文件)— QR-A

**需求(来自 Yang)**:概览页加「策略描述」——支持**手动/AI 输入文字** + **上传描述文件(pdf/word/excel/md)**;Admin 点开某账户时能**看到文字 / 点击查看文件**。

**QR-A 的方案(节点侧,我来做)**:描述挂在**账户**上(概览与 Admin 墙都是账户粒度)。
- 文字:`accounts.description` 字段。
- 文件:存进节点(SQLite BLOB,含文件名/类型/大小),不落散文件,随数据目录走。
- 节点新增接口(都走入站鉴权,远程需 `X-Admin-Token=node.token`):
  - `POST /api/accounts/{id}/description` `{description}` —— 存文字。
  - `GET  /api/accounts/{id}/description` —— 返回 `{description, files:[{id,filename,content_type,size,uploaded_at}]}`。
  - `POST /api/accounts/{id}/files` `{filename, content_base64}` —— 上传文件(限 25MB,仅 pdf/word/excel/md)。
  - `GET  /api/accounts/{id}/files/{file_id}` —— 下载文件原始字节(带正确 Content-Type + Content-Disposition)。
  - `POST /api/accounts/{id}/files/{file_id}/delete` —— 删文件。
- 节点前端:概览加「策略描述」卡(文本框存/改 + 文件上传 + 文件列表查看/删除)。

**@Admin:需要你做(数据传输那块)**:
1. 老板在墙上点开某账户 → 调**节点的** `GET /api/accounts/{id}/description`(带该节点 `node.token`)→ 展示文字 + 文件清单。
2. 老板点某个文件 → Admin **代理**节点的 `GET /api/accounts/{id}/files/{file_id}`(带 `node.token`)把字节流回老板浏览器(你已有 control 代理,扩成支持 GET 透传文件即可),或给一个经你代理的下载链接。文件原始数据在节点,Admin 只透传,不必落库。
3. 这俩接口的契约/字段我落地后会更新到 `APP_HANDOFF.md`,你按那个对接;有疑问在本文件追加 `@QR-A`。

**进度**:QR-A 实现中,完成后在此回执 + 更新 APP_HANDOFF。

— QR-A

### [2026-06-25] 策略描述 — 节点侧已完成(回执)— QR-A

节点侧(app **v1.14.0**)全部落地并实测:
- `accounts.description` 字段 + `account_files` 表(BLOB 存字节);类型(pdf/word/excel/md/txt/csv)+ 大小(≤25MB)校验。
- 接口:`POST/GET /api/accounts/{id}/description`、`POST/GET /api/accounts/{id}/files`、`GET /api/accounts/{id}/files/{file_id}`(原始字节+正确 Content-Type)、`POST .../files/{fid}/delete`。
- `/api/portfolio/summary` 的 `accounts[]` 已带 `description`(你轮询即得文字)。
- 节点前端:概览页「策略描述」卡(文本框存/改 + 上传 + 文件列表查看/删除),已截图验证。
- 195 tests OK。

**@Admin:你的部分(详见 `APP_HANDOFF.md` 末尾「契约 v8」)**:① 墙上展示 `description`(summary 已带);② 文件查看 = 你用 `node.token` 代理 `GET /api/accounts/{id}/files/{file_id}` 透传给老板浏览器(GET + 二进制透传)。做完在此回执 `@QR-A`。

— QR-A
